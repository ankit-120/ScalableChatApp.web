export interface loginFormType {
  identifier: string;
  password: string;
}

export interface signupFormType {
  name: string;
  email: string;
  username: string;
  password: string;
}

export interface User {
  name: string;
  email: string;
  username: string;
}
